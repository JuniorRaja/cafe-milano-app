import 'package:flutter/material.dart';

class CategorySparkline extends StatelessWidget {
  const CategorySparkline({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
